@model IEnumerable<Project2.Models.User>
<table class="AccountTable table table-striped table-hover">
    <thead class="table-thead">
        <tr>
            <th scope="col">#</th>
            <th onclick="sortTable(this)" scope="col">
                User Email <i class="bi bi-funnel-fill"></i>
            </th>
            <th scope="col">Action</th>
        </tr>
    </thead>
    @foreach (var item in Model)
    {
        @if (!item.Role.Name.Equals("admin"))
        {
            <tr>
                <th scope="row">@item.Id</th>
                <td>@item.Email</td>
                <td>
                    <a asp-action="EditUser" asp-controller="Account" asp-route-id="@item.Id">Edit <i class="bi bi-pencil-square"></i></a> |
                    <a href="#" onclick="deleteAccount(this)">Delete <i class="bi bi-trash-fill"></i></a>
                </td>
            </tr>
        }
    }
</table>